import React from 'react';

export const Dashboard: React.FC = () => {
  return (
    <section id="tab-overview" className="tab-pane active">
      {/* The user requested this to be completely empty so they can build from scratch */}
    </section>
  );
};
