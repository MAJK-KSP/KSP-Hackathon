import React from 'react';

export const Dashboard: React.FC = () => {
  return (
    <div className="dash-content">
      {/* Overview content cleared as requested */}
    </div>
  );
};

export default Dashboard;
